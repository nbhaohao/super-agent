# 选哪个 LLM provider（默认 deepseek）。可选：deepseek | anthropic | openai | mock
LLM_PROVIDER=deepseek

# DeepSeek（默认，OpenAI 兼容）—— https://platform.deepseek.com/
DEEPSEEK_API_KEY=

# 其它 provider（按需填，切 LLM_PROVIDER 即用）
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
